# Student_database
A terminal database managing student data using a dynamic Doubly Linked List. Features complete CRUD operations (Add, Delete, Modify, Show) alongside pointer manipulations like in-place list reversal and a custom Merge Sort engine (by Name/Percentage). Includes automated data persistence via standard File I/O for disk saving and loading.
